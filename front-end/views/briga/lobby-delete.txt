<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CineMatch - Lobby da Sessão</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        .delete-user-btn {
            padding: 0.25rem 0.5rem;
            /* Padding pequeno */
            font-size: 0.9rem;
            /* Tamanho do ícone */
            line-height: 1;
            color: #dc3545;
            /* Cor vermelha (perigo) do Bootstrap */
            background-color: transparent;
            border: none;
            /* Sem borda para parecer mais um ícone */
        }

        .delete-user-btn:hover {
            color: #a71d2a;
            /* Vermelho mais escuro no hover */
            background-color: rgba(220, 53, 69, 0.1);
            /* Fundo sutil no hover */
        }

        html {
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #d4aFF7, #f3c2e0, #a8b5f0, #f7b7d7);
            background-size: 400% 400%;
            animation: gradientAnimation 18s ease infinite;
            padding: 20px;
        }
        body{
            border-radius: 15px;
        }
        @keyframes gradientAnimation {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        .lobby-container {
            width: 100%;
            max-width: 500px;
            padding: 30px;
            background-color: rgba(255, 255, 255, 0.92);
            border-radius: 15px;
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.15);
            text-align: center;
        }

        .logo-img {
            max-height: 50px;
            margin-bottom: 20px;
        }

        .lobby-container h1 {
            font-weight: 700;
            color: #3c325c;
            margin-bottom: 15px;
            font-size: 1.8rem;
        }

        .session-code-display {
            font-size: 2.2rem;
            font-weight: bold;
            color: #8A2BE2;
            background-color: #f0e8f9;
            padding: 10px 20px;
            border-radius: 8px;
            margin-top: 5px;
            margin-bottom: 25px;
            display: inline-block;
            border: 1px dashed #c3a0e0;
            letter-spacing: 2px;
            min-height: 60px;
        }

        .user-list-container {
            max-height: 180px;
            overflow-y: auto;
            border: 1px solid #e0c3fc;
            border-radius: 8px;
            padding: 10px;
            /* Reduzido para dar espaço interno aos itens */
            background-color: rgba(248, 249, 250, 0.8);
            margin-bottom: 30px;
            text-align: left;
        }

        #user-list {
            padding-left: 0;
            margin-bottom: 0;
            /* Remove margem inferior da ul */
        }

        #user-list .list-group-item {
            background-color: transparent;
            border: none;
            border-bottom: 1px solid #d4aFF7;
            color: #493e6a;
            font-weight: 500;
            padding: 0.75rem 0.5rem;
            /* Ajustado para acomodar botão */
            display: flex;
            /* Para alinhar nome e botão */
            justify-content: space-between;
            /* Espaço entre nome e botão */
            align-items: center;
            /* Alinhar verticalmente */
        }

        #user-list .list-group-item:last-child {
            border-bottom: none;
        }

        .delete-user-btn {
            padding: 0.25rem 0.5rem;
            /* Padding pequeno para o botão */
            font-size: 0.9rem;
            /* Tamanho do ícone */
            line-height: 1;
            /* Ajusta altura da linha */
            color: #dc3545;
            /* Cor vermelha para perigo */
            background-color: transparent;
            border: none;
        }

        .delete-user-btn:hover {
            color: #a71d2a;
            /* Vermelho mais escuro no hover */
            background-color: rgba(220, 53, 69, 0.1);
            /* Fundo sutil no hover */
        }

        .btn-start-session {
            padding: 12px 0;
            font-size: 1.15rem;
            font-weight: 600;
            border-radius: 8px;
            background-color: #7324bf;
            border-color: #7324bf;
            transition: background-color 0.3s ease, border-color 0.3s ease;
            width: 100%;
        }

        .btn-start-session:hover {
            background-color: #5a1d9a;
            border-color: #5a1d9a;
        }
    </style>
</head>

<body>
    <div class="lobby-container">
        <img src="../assets/img/heart_home.svg" alt="Logo CineMatch" class="logo-img">
        <h1>Código da sua sessão</h1>
        <h3 class="session-code-display" id="sessionCodeDisplay"></h3>

        <h5 class="mt-4 mb-2" style="color: #58458c;">Jogadores na sessão:</h5>
        <div class="user-list-container">
            <ul id="user-list" class="list-group">
            </ul>
        </div>
        <form id="Start_lobby" autocomplete="off">
            <button type="submit" class="btn btn-primary btn-start-session">Começar Sessão</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
    <script src="../assets/js/web_socket_lobby.js"></script>

    <script>
        // Exemplo de como seu web_socket_lobby.js poderia interagir com os novos IDs e classes,
        // incluindo o botão de deletar.
        // Este código é apenas ilustrativo e deve ser adaptado/integrado ao seu script existente.
        document.addEventListener('DOMContentLoaded', function () {
            const sessionCodeDisplay = document.getElementById('sessionCodeDisplay');
            const userListUL = document.getElementById('user-list'); // Renomeado para clareza

            // Exemplo: Atualizando o código da sessão (você receberá isso do seu backend)
            if (sessionCodeDisplay) {
                const codigoDaSessao = "WX89YZ"; // Exemplo, substitua com dados reais
                sessionCodeDisplay.textContent = codigoDaSessao;
            }

            // Exemplo: Adicionando usuários à lista (você fará isso dinamicamente via Socket.IO)
            if (userListUL) {
                const usuariosExemplo = ["Arthur", "Breno", "Charlie", "Diana"]; // Exemplo

                // Função para renderizar a lista de usuários
                function renderUserList(usuarios) {
                    userListUL.innerHTML = ''; // Limpa a lista antes de recriar
                    usuarios.forEach(nomeUsuario => {
                        const li = document.createElement('li');
                        li.className = 'list-group-item d-flex justify-content-between align-items-center';

                        const spanNome = document.createElement('span');
                        spanNome.textContent = nomeUsuario;

                        const deleteButton = document.createElement('button');
                        deleteButton.className = 'btn btn-sm delete-user-btn';
                        deleteButton.setAttribute('aria-label', `Remover ${nomeUsuario}`);
                        deleteButton.innerHTML = '<i class="fas fa-trash-alt"></i>';

                        // Adiciona um atributo de dados para identificar o usuário
                        deleteButton.dataset.username = nomeUsuario;

                        deleteButton.onclick = function () {
                            // Aqui você implementará a lógica para remover o usuário
                            // Isso provavelmente envolverá enviar um evento via Socket.IO para o servidor
                            console.log(`Tentativa de remover o usuário: ${this.dataset.username}`);
                            alert(`FUNCIONALIDADE DE REMOVER "${this.dataset.username}" AINDA NÃO IMPLEMENTADA.`);
                            // Exemplo: socket.emit('remove_user_from_lobby', { sessionCode: codigoDaSessao, usernameToRemove: this.dataset.username });
                        };

                        li.appendChild(spanNome);
                        li.appendChild(deleteButton);
                        userListUL.appendChild(li);
                    });
                }

                renderUserList(usuariosExemplo); // Renderiza a lista de exemplo inicial

                // Você precisará chamar renderUserList(listaDeUsuariosAtualizada)
                // sempre que a lista de usuários mudar (recebido via Socket.IO do seu backend).
            }

            // Seu código existente em web_socket_lobby.js para lidar com o formulário Start_lobby,
            // conexões socket.io, etc., continuará funcionando, pois os IDs foram mantidos.
        });
    </script>
</body>

</html>